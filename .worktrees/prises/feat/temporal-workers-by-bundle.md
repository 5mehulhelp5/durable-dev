# feat/temporal-workers-by-bundle

- **Chantier** : #420 — Temporal workers registered by the bundle from `durable.temporal.dsn`; drop `purpose=`, aliases `durable_workflows` / `durable_activities` / `durable_nexus`.
- **Entrées** : `src/DurableBundle/DependencyInjection/`, `src/Bridge/Temporal/Messenger/`, benches `symfony/` and `sylius/`, user docs EN + FR.
- **État** : en cours
