# docs/the-loop-pauses

- **Work item**: CLAUDE.md's DISPATCH rule binds loop-driven work only, WA007 records the pause
  with a date, `verify.sh` is described as what it is (the two required jobs), and
  `verify-goals.sh` stops editing tracked files as a side effect of a read. Issue #384, day 5 of
  the roadmap #304. Running the loop stays the human's decision.
- **Inputs**: `CLAUDE.md`, `documentation/wa/WA007-*.md`, `loop/verify-goals.sh`, `loop/loop.sh`.
- **State**: in progress.
