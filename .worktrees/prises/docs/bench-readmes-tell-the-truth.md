# docs/bench-readmes-tell-the-truth

- **Work item**: the Magento bench README and probe module, the Laravel bench and package text,
  the PHPStan and Rector READMEs, two Symfony bench comments and the DBAL bridge README describe
  the code as it is — names, attributes, what CI runs, what exists. Issue #358, day 4 of the
  roadmap #304.
- **Inputs**: `magento/`, `laravel/README.md`, `src/DurableLaravel/{README.md,config/durable.php,composer.json}`
  (description text only in the manifest), `src/DurablePhpstan/README.md`, `src/DurableRector/README.md`,
  `src/DurableModule/etc/di.xml`, `symfony/config/`, `src/Bridge/Dbal/README.md`, one new test.
- **State**: in progress.
